.table.list
  - venues.each do |venue|
    - present venue do |venue|
      .tr
        .td
          = link_to venue.full_title, venues_show_path(venue, venue.slug)
        .td.text-right
          = "#{venue.events.count} #{'event'.pluralize(venue.events.count)}"
