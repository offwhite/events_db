$('.profile_lookup_list').html(
  <%- if @profiles.empty? %>
    "<div class='none-found'>Nothing found</div>"
  <%- else %>
    "<%= escape_javascript(render partial: 'profile_lookup_list', locals: { profiles: @profiles } ) %>"
  <%- end %>
).slideDown();
